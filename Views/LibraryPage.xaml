<Page x:Class="SLSKDONET.Views.LibraryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LibraryPage">

    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource SecondaryBackgroundBrush}"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="16">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Label Content="Download History &amp; Imported Queries" Style="{StaticResource ModernLabelStyle}"
                   FontSize="{StaticResource FontSizeMedium}" FontWeight="Bold"/>
                <ui:Button x:Name="ToggleImportedBtn" Content="Show Imported" Icon="{ui:SymbolIcon ChevronDown24}" 
                           Margin="8,0,0,0" Click="ToggleImportedBtn_Click"/>
            </StackPanel>
        </Border>

        <!-- Imported Queries Section (collapsible) -->
        <Expander Grid.Row="1" x:Name="ImportedExpander" Header="Imported Queries" 
                  Background="{StaticResource SecondaryBackgroundBrush}"
                  BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                  IsExpanded="False" Padding="16">
            <StackPanel>
                <DataGrid ItemsSource="{Binding ImportedQueries}"
                      Style="{StaticResource ModernDataGridStyle}"
                      ColumnHeaderStyle="{StaticResource ModernColumnHeaderStyle}"
                      CellStyle="{StaticResource ModernCellStyle}"
                      RowStyle="{StaticResource ModernRowStyle}"
                      Margin="0,0,0,12"
                      Height="150"
                      AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Extended">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Artist" Binding="{Binding Artist}" Width="150"/>
                        <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                        <DataGridTextColumn Header="Album" Binding="{Binding Album}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
                <ui:Button Content="Search All Imported" Icon="{ui:SymbolIcon Search24}" Appearance="Primary"
                           Command="{Binding SearchAllImportedCommand}"/>
            </StackPanel>
        </Expander>

        <DataGrid Grid.Row="2" Name="LibraryGrid" ItemsSource="{Binding LibraryEntries}"
              Style="{StaticResource ModernDataGridStyle}"
              ColumnHeaderStyle="{StaticResource ModernColumnHeaderStyle}"
              CellStyle="{StaticResource ModernCellStyle}"
              RowStyle="{StaticResource ModernRowStyle}"
              Margin="0"
              AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Extended">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Artist" Binding="{Binding Artist}" Width="150"/>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="Album" Binding="{Binding Album}" Width="150"/>
                <DataGridTextColumn Header="Format" Binding="{Binding Format}" Width="70"/>
                <DataGridTextColumn Header="Bitrate" Binding="{Binding Bitrate}" Width="70"/>
                <DataGridTextColumn Header="Downloaded From" Binding="{Binding Username}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3" Background="{StaticResource SecondaryBackgroundBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="16">
            <StackPanel Orientation="Horizontal">
                <ui:Button Content="Remove Selected" Icon="{ui:SymbolIcon Delete24}" Appearance="Danger"
                           Command="{Binding RemoveFromLibraryCommand}"
                           CommandParameter="{Binding ElementName=LibraryGrid, Path=SelectedItems}"/>
                <ui:Button Content="Rescan Folder" Icon="{ui:SymbolIcon ArrowClockwise24}" Margin="8,0,0,0"
                           Command="{Binding RescanLibraryCommand}"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>