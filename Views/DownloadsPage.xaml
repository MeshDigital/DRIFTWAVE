<Page x:Class="SLSKDONET.Views.DownloadsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DownloadsPage"
      Background="{StaticResource BackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource SecondaryBackgroundBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" Grid.Column="0">
                    <ui:Button Content="Start Downloads" Icon="{ui:SymbolIcon Play24}" Appearance="Primary"
                               Command="{Binding StartDownloadsCommand}"/>
                    <ui:Button Content="Cancel All" Icon="{ui:SymbolIcon Dismiss24}" Margin="8,0,0,0"
                               Command="{Binding CancelDownloadsCommand}"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Right">
                    <TextBlock Text="✔ Done:" Foreground="{StaticResource AccentBrush}"/>
                    <TextBlock Text="{Binding SuccessfulCount}" FontWeight="Bold" Margin="4,0,12,0"/>
                    <TextBlock Text="⚠ Failed:" Foreground="{StaticResource ErrorBrush}"/>
                    <TextBlock Text="{Binding FailedCount}" FontWeight="Bold" Margin="4,0,12,0"/>
                    <TextBlock Text="⬇ In Progress:" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock Text="{Binding TodoCount}" FontWeight="Bold" Margin="4,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Downloads}"
              Style="{StaticResource ModernDataGridStyle}"
              ColumnHeaderStyle="{StaticResource ModernColumnHeaderStyle}"
              CellStyle="{StaticResource ModernCellStyle}"
              RowStyle="{StaticResource ModernRowStyle}"
              Margin="0"
              AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Extended">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding Id}" Width="140"/>
                <DataGridTextColumn Header="Source" Binding="{Binding SourceTitle}" Width="150"/>
                <DataGridTextColumn Header="Artist" Binding="{Binding Track.Artist}" Width="140"/>
                <DataGridTextColumn Header="Title" Binding="{Binding Track.Title}" Width="*"/>
                <DataGridTextColumn Header="User" Binding="{Binding Track.Username}" Width="120"/>
                <DataGridTextColumn Header="Dest" Binding="{Binding DestinationPath}" Width="220"/>
                <DataGridTextColumn Header="Status" Binding="{Binding State}" Width="100"/>
                <DataGridTemplateColumn Header="Progress" Width="170">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ProgressBar Value="{Binding Progress}" Maximum="1.0" Height="18" Width="110"/>
                                <TextBlock Text="{Binding Progress, StringFormat={}{0:P0}}" Width="40" HorizontalAlignment="Right" Margin="8,0,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
