<Page x:Class="SLSKDONET.Views.ImportHistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:viewmodels="clr-namespace:SLSKDONET.ViewModels"
      mc:Ignorable="d" 
      d:DataContext="{d:DesignInstance Type=viewmodels:ImportHistoryViewModel}"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="ImportHistoryPage">

    <Grid Background="#121212">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- DataGrid -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Padding="40,30" BorderBrush="#222" BorderThickness="0,0,0,1" Background="#181818">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Import History" FontSize="32" FontWeight="Bold" Foreground="White" Margin="0,0,20,0"/>
                <Button Command="{Binding LoadHistoryCommand}" Content="🔄 Refresh" 
                        Background="Transparent" BorderThickness="0" Foreground="#AAA" Cursor="Hand" VerticalAlignment="Bottom" Margin="0,0,0,4"/>
            </StackPanel>
        </Border>

        <!-- Loading Indicator -->
        <Border Grid.Row="1" Panel.ZIndex="10" Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Text="Loading..." Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18"/>
        </Border>

        <!-- History DataGrid -->
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding ImportHistory}" 
                  SelectedItem="{Binding SelectedJob}"
                  AutoGenerateColumns="False"
                  Background="Transparent"
                  BorderThickness="0"
                  RowBackground="Transparent"
                  Foreground="#DDD"
                  RowHeight="50"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#333"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  Padding="20"
                  SelectionChanged="DataGrid_SelectionChanged">

            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#888"/>
                    <Setter Property="Padding" Value="10,8"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#333"/>
                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="10,0">
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="Transparent"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#1AFFFFFF"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#333"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <!-- Date -->
                <DataGridTextColumn Header="Date" Binding="{Binding CreatedAt, StringFormat=g}" Width="160" Foreground="#AAA"/>

                <!-- Source Name -->
                <DataGridTextColumn Header="Source / Playlist" Binding="{Binding SourceTitle}" Width="*" FontWeight="SemiBold" Foreground="White"/>

                <!-- Type -->
                <DataGridTextColumn Header="Type" Binding="{Binding SourceType}" Width="100" Foreground="#CCC"/>

                <!-- Stats -->
                <DataGridTemplateColumn Header="Progress" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel VerticalAlignment="Center">
                                <Grid Height="4" Margin="0,0,0,4">
                                    <ProgressBar Value="{Binding ProgressPercentage, Mode=OneWay}" Maximum="100" Height="4" 
                                                 Background="#333" BorderThickness="0" Foreground="#4CAF50"/>
                                </Grid>
                                <TextBlock FontSize="11" Foreground="#888">
                                    <Run Text="{Binding SuccessfulCount, Mode=OneWay}" Foreground="#4CAF50"/>
                                    <Run Text="OK /"/>
                                    <Run Text="{Binding FailedCount, Mode=OneWay}" Foreground="#F44336"/>
                                    <Run Text="Fail /"/>
                                    <Run Text="{Binding TotalTracks, Mode=OneWay}" Foreground="#AAA"/>
                                    <Run Text="Total"/>
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Actions -->
                <DataGridTemplateColumn Header="Actions" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.ReImportCommand, RelativeSource={RelativeSource AncestorType=Page}}" 
                                    CommandParameter="{Binding}"
                                    Content="Re-Import"
                                    Background="#222" Foreground="#DDD"
                                    BorderThickness="1" BorderBrush="#444"
                                    Padding="10,4" Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#333"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
